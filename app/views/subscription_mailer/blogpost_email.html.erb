<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h4><% unless @sender.name.blank? %><%= @sender.name %><% else %><%= @sender.username %><% end %> just posted on the blog.</h4>
     <p>
        Link to the blogpost   <%= blog_post_url(@blog, @post) %>
    </p>
    <p>
			<% unless @post.excerpt.blank? %>
      	<p><%= @post.excerpt %></p>
        <p><%= link_to "Read more", blog_post_url(@blog, @post), {:class => "overrideanchor"} %></p>
      <% else %>
        <% post_content = @post.content.gsub(/\r\n?/, "").html_safe %>
        <% if post_content.length <= 200 %>
         	<%= post_content %>
        <% else %>
          <%= post_content.split(" ").first(200).join(" ").html_safe %>...
        	<p><%= link_to "Read more", blog_post_url(@blog, @post), {:class => "overrideanchor"} %></p>
        <% end %>
      <% end %>
    </p>
  </body>
</html>