<%= form_for(@canto) do |f| %>
  <% if @canto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@canto.errors.count, "error") %> prohibited this canto from being saved:</h2>

      <ul>
      <% @canto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantono %><br />
    <%= f.number_field :cantono %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
